username=dabaker3
version=v4
cd ~/pepMeld

tar -czf pepMeld.tar.gz pepMeld
mv -f pepMeld.tar.gz ./pepMeld_docker/pepMeld.tar.gz
cd ~/pepMeld/pepMeld_docker
docker build -t pepmeld:${version} .
docker run --cpus 2 -it -v ~/pepMeld/COVID_19/human/igg:/scratch pepmeld:${version}

mkdir -p /scratch/out
python /pepMeld/process_arrays.py --transforms_path=/scratch/igg_transformations.json

exit

docker tag pepmeld:${version}  dockerreg.chtc.wisc.edu/${username}/pepmeld:${version}
docker push dockerreg.chtc.wisc.edu/${username}/pepmeld:24048


